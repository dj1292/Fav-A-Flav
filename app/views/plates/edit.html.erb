<%= form_for @plate, method: :patch do |f| %>
<%= f.label :name %>
<%= f.text_field :name %> <br>
<%= f.label :category %>
<%= f.text_field :category %> <br>
<b><%= f.label :username %></b>
<%= f.collection_select(:user_id, User.all, :id, :username) %> <br>
<%= f.submit %>
<% end %>